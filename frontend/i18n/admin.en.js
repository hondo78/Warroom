window.I18N = window.I18N || {}; window.I18N.en = window.I18N.en || {};
window.I18N.en.admin = {
    agentLanguage: "Agent language (prompts/chat/Telegram)",
    agentLanguageHint: "Language of the LLM default prompts, the analyst chat replies and the Telegram messages. Custom (overridden) prompts are left untouched.",
    // page + section titles
    pageTitle: "Admin",
    sophosTitle: "Sophos Central API",
    o365Title: "Microsoft 365 audit log",
    entraTitle: "Entra ID — login blocking (Conditional Access)",
    telegramTitle: "Telegram — notifications & approvals",
    teamsTitle: "Microsoft Teams — command channel",
    analystTitle: "AI analyst — persona (system prompt)",
    iocTitle: "IOC delivery to the firewall",
    geoipTitle: "GeoIP & OSINT sources",
    quotaTitle: "OSINT providers — quota limits",
    agentTitle: "🤖 AI agent (alert analysis)",
    systemPromptsTitle: "🤖 AI agent — system prompts",
    generalTitle: "General",
    retentionTitle: "🧹 Firewall log retention",
    systemTitle: "System (read-only)",

    // shared placeholder
    secretUnchangedPlaceholder: "(leave unchanged to keep the current value)",

    // Sophos
    sophosHint: "OAuth2 credentials from the Sophos Central API Token Management. The tenant ID is detected automatically after login if left empty.",
    clientId: "Client ID",
    clientSecret: "Client Secret",
    tenantIdOptional: "Tenant ID (optional)",
    saveSophos: "Save Sophos settings",

    // M365
    hostnamesTitle: "Internal hostnames",
    hostnamesHint: "Resolves hostnames for internal (private) IPs and shows them wherever an internal IP appears. Sources in order: <strong>Sophos endpoints</strong> → <strong>reverse DNS (PTR)</strong> → <strong>NetBIOS</strong> → <strong>firewall DHCP</strong>. For PTR of private ranges, enter your internal DNS server IP (the container's default resolver usually can't).",
    hostnamesEnabled: "Resolution active",
    internalDnsServers: "Internal DNS servers",
    internalDnsServersHint: "Comma-separated. Empty ⇒ system resolver. PTR records must exist.",
    netbiosEnabled: "NetBIOS fallback (UDP 137)",
    hostnameTtl: "Cache TTL (hours)",
    hostnameNegTtl: "Negative TTL (hours)",
    saveHostnames: "Save hostname settings",
    fwDhcpTitle: "Firewall DHCP (SFOS XML API)",
    fwDhcpHint: "Reads DHCP mappings (IP → client name) from the Sophos firewall and adds them as the hostname source \"dhcp\". The API user must be API-enabled on the firewall and its access allowed for Warroom's IP.",
    fwDhcpEnabled: "DHCP reconcile active",
    fwApiHost: "Firewall host",
    fwApiPort: "API port",
    fwApiUser: "API user",
    fwApiPassword: "API password",
    fwApiVerifyTls: "Verify TLS certificate",
    fwDhcpEntity: "DHCP entity",
    fwDhcpEntityHint: "XML API entity (comma-separated allowed). Firmware-dependent; the default returns reserved mappings.",
    fwDhcpRefresh: "Refresh (sec)",
    fwDhcpTest: "Test connection",
    fwDhcpTesting: "Testing…",
    fwDhcpOk: "OK — {n} mapping(s)",
    fwDhcpZero: "Connected, but 0 mappings — see the raw response below (different entity name, or no static DHCP reservations).",
    o365Hint: "Entra ID app registration with admin consent. The collector fetches login events (UserLoggedIn / UserLoginFailed); failed attempts also appear as alerts on the attack map. Shown under <a href=\"/o365.html\" class=\"osint-link\">Microsoft 365</a>.<br><strong>Required application permissions:</strong><br>• <code>ActivityFeed.Read</code> (<em>Office 365 Management APIs</em>) — login audit log.<br>• <code>User.RevokeSessions.All</code> (<em>Microsoft Graph</em>) — for the login watch: revoke sessions on sign-in from a new device/location.",
    tenantId: "Tenant ID",
    saveO365: "Save M365 settings",

    // Entra
    syncNow: "Sync now",
    entraHint: "Pushes blocked IPs into an Entra <em>Named Location</em> bound to a Conditional Access policy — M365 logins from these IPs are then rejected directly at Microsoft. Uses the same app registration as the M365 collector, but additionally requires the Graph permissions <code>Policy.ReadWrite.ConditionalAccess</code> + <code>Policy.Read.All</code> (application, admin consent). <br><strong>Security:</strong> The policy is created in <em>report-only</em> mode — you must deliberately enable enforcement in the Entra portal.",
    namedLocationSyncActive: "Named Location sync active",
    syncIntervalMinutes: "Sync interval (minutes)",
    excludedUsers: "Excluded users (break-glass)",
    excludedUsersPlaceholder: "emergency@yourdomain.com, another-objectid, …",
    excludedUsersHint: "Required for <em>automatic</em> policy creation — UPN(s) or object ID(s), comma-separated. These accounts are never blocked (protection against locking yourself out).",
    saveEntra: "Save Entra settings",
    caPolicyEnforcement: "Conditional Access policy (enforcement)",
    statusLoading: "Loading status…",

    // Telegram
    sendTestMessage: "Send test message",
    telegramHint: "Sends every open agent decision as a message with <strong>✅ Approve / ❌ Reject</strong> buttons to a Telegram chat; the decision is executed directly from Telegram (long polling, no public webhook needed). Create a bot via <code>@BotFather</code>, get the <code>chat_id</code> e.g. via <code>@userinfobot</code> and message the bot once / add it to the group first.",
    enabled: "Enabled",
    botToken: "Bot Token",
    chatId: "Chat ID",
    pollIntervalSeconds: "Poll interval (sec.)",
    saveTelegram: "Save Telegram settings",

    // Teams
    teamsHint: "Allows commands from Teams (block, isolate, quarantine, OSINT, statistics) — the same AI as in the <a href=\"/chat.html\" class=\"osint-link\">AI Chat</a>. In Teams create an <strong>Outgoing Webhook</strong> (Team → … → Manage → Outgoing Webhooks), enter <code>https://&lt;warroom&gt;/api/teams/command</code> as the callback URL and store the <strong>HMAC secret</strong> generated by Teams here. The bot then responds to <code>@BotName &lt;command&gt;</code>.",
    outgoingWebhookSecret: "Outgoing webhook HMAC secret",
    incomingWebhookUrl: "Incoming webhook URL (notifications, optional)",
    saveTeams: "Save Teams settings",

    // Analyst persona
    loadDefault: "Load default",
    analystHint: "System prompt for the <strong>free chat</strong> with the LLM (via <a href=\"/chat.html\" class=\"osint-link\">AI Chat</a>, Teams and Telegram). Unrecognized commands are answered as a conversation with this analyst persona. Empty = built-in default prompt. Requires an enabled agent (LLM endpoint).",
    analystPlaceholder: "Leave empty for the default analyst persona…",
    chatSqlLabel: "<strong>Database read access</strong> — the chat may run read-only SQL (SELECT only) against Postgres to answer data questions with real values. <span class=\"text-secondary\">app_settings (secrets) stays locked.</span>",
    savePersona: "Save persona",

    // IOC delivery
    mdrPushTest: "Push/test MDR feed now",
    verifyLastPush: "Verify last push",
    iocHint: "Two independent ways to deliver the blocklists to the firewall — both can be active at the same time. To maintain the lists see the <a href=\"/blocked.html\" class=\"osint-link\">blocklist page</a>.",
    threatFeedsPull: "1) Threat feeds (pull, TXT)",
    threatFeedsPullHint: "The firewall fetches the blocklists itself as TXT feeds (one entry per line) — configured as a third-party threat feed, with an <code>X-API-Key</code> header. Off ⇒ the feeds return an empty body (a polling firewall thereby clears its list).",
    ipFeed: "IP feed:",
    domainFeed: "Domain feed:",
    urlFeed: "URL feed:",
    mdrThreatFeedPush: "2) MDR threat feed (push, API)",
    mdrThreatFeedPushHint: "Warroom actively pushes the blocklists to the firewall's MDR threat feed via the Sophos Central firewall API (<code>POST /firewall/v1/firewalls/{id}/mdr-threat-feed/indicators</code>). Uses the Sophos Central credentials configured above.",
    mdrFirewallIds: "MDR: firewall IDs (optional)",
    mdrFirewallIdsPlaceholder: "empty = all Central-managed firewalls; otherwise comma-separated IDs",
    mdrFirewallIdsHint: "Leave empty to push to all firewalls reported by Sophos Central.",
    mdrSyncIntervalSeconds: "MDR: sync interval (seconds)",
    saveIoc: "Save IOC delivery",

    // GeoIP / OSINT
    testAbuseipdb: "Test AbuseIPDB",
    geoipHint: "API keys for GeoIP enrichment and OSINT lookups. GreyNoise Community + ipinfo do not need a key.",
    maxmindLicenseKey: "MaxMind License Key",
    abuseipdbApiKey: "AbuseIPDB API Key",
    virustotalApiKey: "VirusTotal API Key",
    shodanApiKey: "Shodan API Key",
    shodanAutoEvery: "Shodan: auto query on EVERY OSINT run",
    shodanAutoEveryHint: "Enabled → every OSINT IP lookup additionally queries Shodan (consumes a credit each time). Off → Shodan stays credit-frugal (only manual + the exception below).",
    shodanAutoMalicious: "Shodan: auto query on malicious IP",
    shodanAutoMaliciousHint: "Shodan credits are scarce: queries only manually (OSINT button/triage). Enabled → automated agent loops may additionally query Shodan for clearly malicious IPs.",
    maliciousThreshold: "\"Malicious\" threshold (AbuseIPDB %)",
    intelixClientId: "Sophos Intelix Client ID",
    intelixClientSecret: "Sophos Intelix Client Secret",
    saveOsint: "Save OSINT settings",

    // Quota
    showStatistics: "Show statistics",
    quotaHint: "Enter your contractual daily and monthly limit per provider here. <code>/stats.html</code> uses these values for the utilization bars and warning badges. <strong>0</strong> = no limit (provider ignores this window). Defaults match the free tiers.",
    abuseipdbDay: "AbuseIPDB · day",
    abuseipdbMonth: "AbuseIPDB · month",
    virustotalDay: "VirusTotal · day",
    virustotalMonth: "VirusTotal · month",
    shodanDay: "Shodan · day",
    shodanMonth: "Shodan · month",
    greynoiseDay: "GreyNoise · day",
    greynoiseMonth: "GreyNoise · month",
    intelixDay: "Sophos Intelix · day",
    intelixMonth: "Sophos Intelix · month",
    ipapiDay: "ip-api.com · day",
    ipapiMonth: "ip-api.com · month",
    saveQuota: "Save quota limits",

    // Agent
    loadModels: "Load models",
    analyzeNow: "Analyze now",
    agentHint: "The agent periodically pulls in new alerts, calls an LLM (OpenAI-compatible — LMStudio, Ollama, vLLM, OpenAI) and proposes an action. Recommendations land as <em>pending</em> in the dashboard list until a human approves them — unless auto-execute is off.",
    agentActive: "Agent active",
    provider: "Provider",
    providerLmstudio: "LMStudio (local)",
    providerOllama: "Ollama (local)",
    providerOpenai: "OpenAI / compatible",
    baseUrl: "Base URL",
    apiKey: "API key",
    apiKeyPlaceholder: "(LMStudio: leave empty)",
    model: "Model",
    modelPlaceholder: "e.g. llama-3.1-8b-instruct",
    availableModels: "— available models —",
    analysisIntervalSeconds: "Analysis interval (seconds)",
    llmTemperature: "LLM temperature",
    llmTemperatureHint: "0 = deterministic/consistent, higher = more creative. Keep low for security decisions (0–0.3).",
    llmMaxTokens: "LLM max response tokens",
    llmMaxTokensHint: "Upper limit of the response length. Reasoning models need headroom for their internal \"think\" block — keep generous (≈3000).",
    autoExecute: "Auto-execute (acknowledge only)",
    autoExecuteHint: "When active, the agent only executes non-destructive actions (acknowledge) without approval. Block actions and isolate always remain <strong>manual</strong>. The action follows solely from the thresholds per source — there is no confidence calculation anymore.",
    centralEventAgent: "Central event agent (rule-based)",
    centralEventAgentHint: "Evaluates the Sophos Central <strong>event stream</strong> (separate from alerts) — endpoint threat/C2/exploit events. For each new event of the configured types the LLM decides <code>block_ip</code> / <code>isolate</code> / <code>acknowledge</code>.",
    eventTypes: "Event types (filter, comma-separated)",
    eventTypesHint: "Only events with these <code>event_type</code> values go to the LLM. Firewall ATP events are deliberately excluded (covered via WAF/IPS).",
    eventIntervalSeconds: "Event interval (seconds)",
    wafAgent: "WAF agent (rule-based)",
    wafAgentHint: "When active, a separate loop checks every new WAF line with 4xx/5xx status per source IP. Rule: <strong>≥ threshold fail hits in 24 h</strong> OR <strong>poor OSINT reputation</strong> ⇒ <code>block_ip</code>. Decisions land in the audit trail; whitelisted IPs are discarded anyway.",
    wafFailThreshold: "WAF fail threshold (24 h)",
    wafFailThresholdHint: "Number of 4xx/5xx hits from the same IP in 24 h above which it is blocked (default 4).",
    wafIntervalSeconds: "WAF interval (seconds)",
    ipsAgent: "IPS agent (rule-based)",
    ipsAgentHint: "Checks every new IDP/IPS event per source IP. Rule: <strong>severity = high/critical</strong> ⇒ block_ip immediately; otherwise <strong>≥ threshold hits in 24 h</strong> OR <strong>poor OSINT reputation</strong> ⇒ <code>block_ip</code>.",
    ipsHitThreshold: "IPS hit threshold (24 h)",
    ipsIntervalSeconds: "IPS interval (seconds)",
    failedLoginAgent: "Failed-login agent (rule-based)",
    failedLoginAgentHint: "Detects brute-force attempts. Rule: <strong>≥ threshold failed logins in 24 h per IP</strong> OR <strong>poor OSINT reputation</strong> ⇒ <code>block_ip</code>.",
    failedLoginThreshold: "Failed-login threshold (24 h)",
    failedLoginIntervalSeconds: "Failed-login interval (seconds)",
    subnetBfAttempts: "Subnet brute-force / attempts",
    subnetBfAttemptsHint: "If a /24 network sends a total of ≥ N failed-login attempts in 24 h AND the attempts come from ≥ M distinct IPs, <strong>all</strong> active IPs of this subnet are blocked. Defense against coordinated botnets.",
    subnetBfMinIps: "Subnet brute-force / min. distinct IPs",
    distributedBfActive: "Distributed brute-force active",
    distributedBfActiveHint: "Passes <strong>all</strong> failed login attempts of the time window as JSON to the AI agent. It groups them itself by /24 network, counts per /24 and decides whether a distributed brute-force is occurring (→ block the /24 or the suspicious IPs). Applies exclusively to login logs.",
    distributedBfWindow: "Distributed BF / time window (minutes)",
    distributedBfWindowHint: "Which login attempts the agent sees (default 60 minutes).",
    distributedBfAttempts: "Distributed BF / attempts per /24 (guideline)",
    distributedBfMinIps: "Distributed BF / distinct IPs per /24 (guideline)",
    distributedBfMinIpsHint: "Guideline values for when a network counts as coordinated — passed to the agent as a hint; it may deviate with justification for a clear pattern.",
    networkBlock: "Block whole network (OSINT/ipinfo)",
    networkBlockHint: "Instead of a naive /24, the <strong>actual network (CIDR)</strong> is determined per suspicious network via OSINT/ipinfo RDAP. If multiple distinct IPs from the same network attack, the <strong>whole network</strong> is proposed (block requires approval; overly large networks are downgraded to a single-IP block). Off ⇒ classic /24 grouping.",
    saveAgent: "Save agent settings",

    // System prompts card
    systemPromptsHint: "The system prompts per stage (alerts, WAF, IPS, failed-login, distributed brute-force, triage) are now edited in the context of each workflow stage — together with trigger, thresholds, allowed actions and auto-execute.",
    toWorkflowPage: "To the agent workflow page",

    // General
    collectorIntervalSeconds: "Collector interval (seconds)",
    logLevel: "Log level",
    dashboardTitle: "Dashboard title",
    saveGeneral: "Save general settings",

    // Retention
    retentionHint: "The <code>firewall_logs</code> table grows fast — the high-volume <code>Firewall</code> connection logs are deleted earlier than security-relevant types (WAF/IPS/Auth/ATP). Deletion runs batchwise in the background (every 6 h) without write locks.",
    retentionActive: "Retention active",
    connectionLogsRetentionDays: "Keep connection logs (days)",
    connectionLogsRetentionHint: "Applies to <code>log_type='Firewall'</code> (connection tracking — typically ~94% of the volume). NetFlow covers traffic analysis separately.",
    securityLogsRetentionDays: "Keep security logs (days)",
    securityLogsRetentionHint: "Applies to all other types (WAF, IPS/IDP, Authentication, ATP, …).",
    cleanupNow: "Clean up now",
    saveRetention: "Save retention",

    // System (read-only)
    systemHint: "These values are container-bound and changed via <code>.env</code> + container restart.",
    databaseUrl: "Database URL:",
    redisUrl: "Redis URL:",
    warroomApiKey: "Warroom API Key:",
    warroomApiKeyHint: "(set in <code>.env</code>, restart the frontend container — nginx injects the header at boot)",

    // JS strings
    caM365NotConfigured: "M365 app not configured.",
    caNoPolicyError: "No policy found ({error}).",
    caNoPolicy: "No Warroom policy found. Create it in the Entra portal or use \"Sync now\".",
    enforcementOn: "Enforcement ON",
    enforcementOff: "Enforcement OFF",
    stateEnforcing: "enforcing",
    stateDisabled: "disabled",
    caPolicyStatus: "Policy: <strong>{name}</strong> &nbsp; {badge}",
    statusUnavailable: "Status unavailable: {error}",
    enforcePrompt: "Enable enforcement — excluded accounts (break-glass) that are never blocked.\nUPN(s) or object ID(s), comma-separated. At least one is required:",
    breakGlassRequired: "At least one break-glass account required — activation aborted.",
    policyEnabled: "Policy enabled (enforcing).",
    policyDisabled: "Policy disabled.",
    toggleFailed: "Toggle failed: {error}",
    loadError: "Load error: {error}",
    secretStored: "••• stored (leave empty to keep unchanged)",
    secretNotSet: "Not set",
    apiKeySet: "set",
    apiKeyEmpty: "empty (open mode)",
    noChanges: "No changes.",
    saved: "Saved: {items}",
    saveError: "Save error: {error}",
    testing: "Testing {target}…",
    testTenant: " Tenant: {tenant}",
    testOk: "✓ {target} OK.{detail}",
    testFail: "✗ {target}: {error}",
    testError: "Test error: {error}",
    unknownError: "unknown error",
    mdrPushing: "Pushing blocklists to the MDR threat feed…",
    mdrSkipped: "Skipped: {reason}",
    mdrNoTargets: "MDR push: no target firewalls ({note}).",
    mdrPushFailed: "MDR push failed: {details}",
    mdrPartial: "MDR push: {pushed} pushed, {rejected} value(s) rejected",
    mdrPartialFailed: ", {n} firewall(s) with error",
    mdrPushOk: "✓ MDR push: {pushed} indicator(s) to {n} firewall(s).",
    mdrPushFailedErr: "MDR push failed: {error}",
    mdrVerifying: "Checking transactions of the last MDR push…",
    mdrVerifyError: "Verification: {error}",
    mdrVerifyNone: "Verification: no transactions found.",
    mdrVerifyPart: "{id}…: {completed}/{total} applied",
    mdrVerifyPending: ", {n} pending",
    mdrVerifyFailedPart: ", {n} failed",
    mdrVerifyResult: "MDR verification (push {at}): {parts}",
    mdrVerifyFailed: "Verification failed: {error}",
    entraSyncing: "Entra sync running…",
    entraSyncOk: "✓ {ranges} IP range(s) synced to Entra.",
    entraSyncFailed: "Entra sync failed: {error}",
    personaLoaded: "Default persona loaded — save to apply.",
    personaLoadFailed: "Loading default persona failed: {error}",
    loadingModels: "Loading models from the agent endpoint…",
    modelListFailed: "Model list failed: {error}",
    modelsFound: "{n} model(s) found.",
    errorGeneric: "Error: {error}",
    retentionConfirm: "Run firewall log retention now? Old logs are deleted batchwise in the background (may take a few minutes depending on the backlog).",
    retentionStarted: "Retention run triggered — deletion runs in the background.",
    agentRunStarted: "Agent run triggered. Recommendations will appear in the dashboard.",
};
